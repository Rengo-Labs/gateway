version: 1
configurations:
  $namespace server:
    configurationPath: server.yml
    schemaPath: server-schema.json

  $namespace casper:
    configurationPath: casperswap.yml
    schemaPath: casperswap-schema.json